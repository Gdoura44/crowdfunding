@startuml
title Séquence métier — Rechercher un projet (Project)
' ------------------------------------------------------------------
' Style impression A4 (PFE FSEG) — lisibilité : polices ~11–12 pt visibles,
' DPI 240, retour à la ligne des messages. Inclure juste après la ligne title.
' Export PNG pour Word/LaTeX : viser largeur <= zone utile A4 (~17 cm).
' ------------------------------------------------------------------
scale max 1550 width
skinparam dpi 240
skinparam shadowing false
skinparam handwritten false
skinparam HyperlinkUnderline false

skinparam defaultFontSize 12
skinparam titleFontSize 16
skinparam ArrowFontSize 11
skinparam arrowThickness 1.25
skinparam wrapWidth 220
skinparam NoteFontSize 10
skinparam LegendFontSize 10
skinparam footerFontSize 10
skinparam headerFontSize 10

skinparam responseMessageBelowArrow true
skinparam sequenceMessageAlign center
skinparam ParticipantPadding 14
skinparam BoxPadding 8
skinparam LifeLineBorderColor #444444
skinparam LifeLineBackgroundColor #FEFEFE
skinparam ArrowColor #222222
skinparam ActorBorderColor #222222
skinparam ActorBackgroundColor #EEF6FF
skinparam ParticipantBorderColor #333333
skinparam ParticipantBackgroundColor #FFFFFF

skinparam DatabaseBackgroundColor #F8F8F8
skinparam EntityBackgroundColor #F8FFF8
skinparam ControlBackgroundColor #FFF8F0
skinparam BoundaryBackgroundColor #F5F5FF
actor "Utilisateur" as user
boundary "CatalogueProjets" as UI
control "Contrôleur" as Svc
entity "Base de données" as DB

user -> UI: Saisir une recherche + filtres (catégorie, niveau de risque, etc.)
UI -> Svc: GET /api/projects/search?q=...&category=...&riskLevel=...&minFunding=...&status=ACTIVE

Svc -> DB: Construire la requête MongoDB
note right of Svc
  - Recherche texte (index) sur title + description
  - Filtres : catégorie, niveau de risque, objectifs (range)
  - Statut filtrable : par défaut `ACTIVE` ; valeur acceptée : ACTIVE ou CLOSED
  - Tri : si $text → textScore + createdAt ; sinon createdAt
end note
Svc -> DB: Project.find(mongoQuery)\n.sort(sort)\n.limit(limit)
DB --> Svc: projects
Svc --> UI: projects
UI --> user: Afficher les résultats
@enduml
